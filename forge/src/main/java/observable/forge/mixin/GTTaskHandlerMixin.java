package observable.forge.mixin;

/**
 * Legacy source placeholder kept so the v3 archive can safely overwrite the
 * v1/v2 source tree. GT Odyssey v3 compatibility deliberately applies NO
 * mixin to GTCEu/GTO classes because GTOLib rejects transformed GT classes.
 */
public final class GTTaskHandlerMixin {
    private GTTaskHandlerMixin() {
    }
}
